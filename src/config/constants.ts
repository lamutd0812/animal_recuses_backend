// Constants
export const ROLES_KEY = 'roles';

// Enums
export enum ERole {
  ADMIN = 'Admin',
  VOLUNTEER = 'Volunteer',
  USER = 'User',
  CLINIC = 'Clinic',
}

export enum ESort {
  ASC = '1',
  DESC = '-1',
}

export enum EStaticDirectory {
  IMAGES = 'images',
  DOCUMENTS = 'documents',
}
